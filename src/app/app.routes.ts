import { Routes } from '@angular/router';
import { DiagramCanvasComponent } from './core/components/diagram-canvas/diagram-canvas.component';

export const routes: Routes = [
    {path:'',component:DiagramCanvasComponent}
];
